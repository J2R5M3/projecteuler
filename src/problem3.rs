use std::num;

fn factor_primes(x: u64)
{
    let bounds = (x as f64).sqrt().ceil() as u64;

    for x in 1..bounds
    {
        
    }
    
    
    let mut primes_vec = Vec::new()
    
    num::sqrt
}

fn era_sieve(n: u64)
{
    let bounds = (x as f64).sqrt().ceil() as u64;
    let mut p = 2;
    
    let mut primes_vec = Vec<bool>::with_capacity(bounds + 1);
    let mut consec_nums_vec = Vec::with_capacity(bounds + 1);

    for x in 1..bounds.step_by(2)
    {
        consec_nums_vec.push(x);
        
    }

    for x in 2..bounds.step_by(p)
    {
        
    }
    
    

    
    num::sqrt
}

fn mark_multiples(primes_vec: &mut Vec<bool>)
{
    
}
